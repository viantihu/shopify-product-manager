node_modules

# macOS
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*





/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Hide files auto-generated by react router
.react-router/
# Local AI-assistant config (not shared)
.claude/
.cursor/
.gemini/

# Per-session git worktrees (see scripts/new-session.mjs). Each parallel dev
# session works in its own .worktrees/<branch> folder off the shared .git.
/.worktrees/

# Internal design docs and plans (IP — never committed to the public repo)
/docs-private/

# Keep the example template tracked (it has no secrets); real .env stays ignored
!.env.example
